#include <behaviors/num_word.dtsi> // Requires auto-layer module.

// Tap: space | Shift + tap: dot -> space -> sticky shift | Hold: nav layer.
ZMK_HOLD_TAP(lt_spc, bindings = <&mo>, <&spc_morph>; flavor = "balanced";
             tapping-term-ms = <300>; quick-tap-ms = <QUICK_TAP_MS>;)
SIMPLE_MORPH(spc_morph, SFT, &kp SPACE, &dot_spc)
ZMK_MACRO(dot_spc, bindings = <&kp DOT &kp SPACE &sk LSHFT>; wait-ms = <0>;
          tap-ms = <5>;)


// Tap: num-word | double-tap: sticky num-layer | Hold: num-layer.
#define SMART_NUM &smart_num NUM 0
ZMK_HOLD_TAP(smart_num, bindings = <&mo>, <&num_dance>; flavor = "balanced";
             tapping-term-ms = <300>; quick-tap-ms = <QUICK_TAP_MS>;)
  ZMK_TAP_DANCE(num_dance, bindings = <&num_word NUM>, <&sl NUM>;
                tapping-term-ms = <300>;)


// Hold: shift |
// Tap: repeat after alpha, else sticky-shift |
// Shift + tap/ double-tap: caps-word
// - The first tap activates sticky-shift,
// - The second tap is thus shift + tap

// TODO Use mod-morph with single-sided shift on same side as the thumb instead of tap-dance. This leaves open shortcuts with modifiers on the opposite side of the keyboard
#define MAGIC_SHIFT &magic_shift 0 SPC
ZMK_HOLD_TAP(magic_shift, bindings = <&magic_shift_hold>, <&spc_dance>;
             flavor = "balanced"; tapping-term-ms = <300>;
             //quick-tap-ms = <QUICK_TAP_MS>;
             )
ZMK_MOD_MORPH(magic_shift_hold, bindings = <&kp LSHFT>, <&caps_word>;
              mods = <(MOD_LSFT)>;)
ZMK_TAP_DANCE(spc_dance, bindings = <&kp SPACE>, <&dot_spc>;
              tapping-term-ms = <150>;)
&caps_word { // Mods deactivate caps-word, requires PR #1451. [TODO: rebase]
  /delete-property/ ignore-modifiers;
};